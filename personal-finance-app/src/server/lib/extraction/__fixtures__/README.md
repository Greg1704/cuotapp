Respuestas guardadas del modelo, para que el transporte `fixture` corra la cadena entera
sin red ni API key. El nombre de cada archivo es `requestKey(texto)` (ver `purchase.ts`).

No se inventan a mano: son las respuestas reales de la primera corrida del corpus.
